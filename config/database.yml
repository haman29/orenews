default: &default
  adapter: mysql2
  encoding: utf8
  host: localhost
  username: root
  password:

development:
  <<: *default
  database: orenews-development

test:
  <<: *default
  database: orenews-test

production:
  <<: *default
  database: orenews-production
